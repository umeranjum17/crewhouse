{
  "name": "crewhouse",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first crew of persistent AI teammates, run by Chief on your own signed-in agent CLIs.",
  "license": "Apache-2.0",
  "type": "module",
  "engines": { "node": ">=22.18" },
  "scripts": {
    "start": "node src/main.ts",
    "build:web": "node scripts/build-web.mjs",
    "test": "node --test test/*.test.ts"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "esbuild": "^0.28.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "typescript": "^5.7.0"
  }
}
