{
  "id": "media",
  "name": "ffmpeg",
  "provides": "Render, cut and convert video, audio and images",
  "kind": "cli",
  "bins": [
    "ffmpeg",
    "ffprobe"
  ],
  "license": "LGPL-2.1+/GPL (detected, never bundled)",
  "source": "system",
  "install": {
    "system": "sudo pacman -S ffmpeg  |  sudo apt install ffmpeg  |  brew install ffmpeg"
  },
  "asks": [
    "never: it only works on files"
  ],
  "allow": [
    "Bash(ffmpeg *)",
    "Bash(ffprobe *)"
  ],
  "grant": {
    "default": false
  }
}
